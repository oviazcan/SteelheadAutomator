{
  "_doc": "Secuencias MÍNIMAS de navegación para que el frontend dispare cada op y capturemos su sha256Hash + la respuesta real. Organizado por pantalla (una navegación cubre varias ops). captures = ops que la receta debe capturar. La validación NO re-ejecuta el hash: usa la respuesta que el frontend ya obtuvo (si trajo data sin errors → el hash es válido). Por eso ya no se necesitan validateVars/expectShape.",
  "_domainDefault": "344",
  "_pendientes": "GetPurchaseOrder (abrir una PO individual) y SensorDashboardQuery (abrir un dashboard de sensor específico) aún sin receta estable — el sistema las marca noCapturado y escala.",
  "recipes": {
    "app-home": {
      "steps": [{ "goto": "/Domains/{domain}" }],
      "captures": ["CurrentUser"]
    },
    "customers-list": {
      "steps": [{ "goto": "/Domains/{domain}/Customers" }],
      "captures": ["AllCustomers"]
    },
    "customer-detail": {
      "steps": [
        { "goto": "/Domains/{domain}/Customers" },
        { "clickFirst": "a[href*='/Customers/']", "hrefMatches": "/Customers/\\d+" }
      ],
      "captures": ["Customer"]
    },
    "sensor-dashboards": {
      "steps": [
        { "goto": "/Dashboards" },
        { "clickFirst": "a[href*='ashboard']", "hrefMatches": "\\d" }
      ],
      "captures": ["AllSensorDashboards"]
    }
  }
}
