# Seguridad — estado, pendientes del audit pre-producción y lo que no aplica

> **Origen:** extraído de `CLAUDE.md` el 2026-08-04 para bajar el costo de arranque de sesión.
> `CLAUDE.md` conserva el resumen de una línea por ítem y el enlace aquí.
>
> Documento hermano: [`../applets/security-integrity-signing.md`](../applets/security-integrity-signing.md)
> (firma ECDSA P-256 + GCP KMS, la bitácora del item 1).

## Seguridad (estado y pendientes)

### Ya implementado (2026-04-14, actualizado 2026-05-14)
- `hash-scanner.js` sanitiza `variablesSamples` con redacción **key-level** (recursiva: `body`, `rawBody`, `html`, `htmlBody`, `token`, `accessToken`, `authToken`, `emailData`, ...), strip de `?token=` en URLs, truncado > 500 chars, y re-saneado en `mergeResults`. (El antiguo op-level denylist `email|invoice|send|...` se quitó en 0.6.23 — era demasiado ancho; la protección real viene de la key-level. Ver `docs/applets/hash-scanner.md`.)
- `responseSamples` y `errorSamples` aplican el mismo `sanitizeValue` recursivo antes de guardarse.
- `.gitignore` cubre `scan_results_*.json` y `~$*.xlsm/xlsx`.
- Historial git purgado de `scan_results_*.json` (filter-repo + force-push a main/gh-pages/feat/po-comparator). Ticket abierto a GitHub Support para GC de commits huérfanos (`789375b`, `c37c9e8`).

### Pendientes del audit pre-producción (por prioridad)
1. **Integridad de scripts remotos (ALTO) — ✅ FASE 0/1/2 HECHAS (2026-07-17):** `extension/background.js` ejecutaba `new Function(c)()` sobre código de GitHub Pages sin verificar. **Solución: firma criptográfica ECDSA P-256** (no solo hashes-en-config, que no detiene un compromiso de repo/Pages). `config.json` firmado (`config.sig` separado) + `scriptIntegrity` por script; verificación fail-closed en `background.js`; privada en **GCP KMS del cliente** (`steelhead-ecoplating`, handoff = cambio de IAM), pública embebida. **Estado:** Fase 0 (KMS provisionado) + Fase 1 (deploy firmado, config 1.7.145) + Fase 2 (extensión republicada con pública embebida, ext 1.6.6) — todo verificado en vivo. **Único pendiente: rollout gradual** — cada máquina activa la verificación fail-closed al actualizar al zip 1.6.6 (las que no actualizan siguen fail-open; break-glass por popup). Ver bitácora [`docs/applets/security-integrity-signing.md`](../applets/security-integrity-signing.md).
2. **XSS vía `innerHTML` (MEDIO) — ✅ HECHO EN WORKBENCH (2026-07-15, pendiente deploy).** Barrido repo-wide: 32 sitios RIESGO-ALTO (nombres de PN/spec/cliente/reporte/label de GraphQL —vector cross-user— o de CSV/XLSX) ahora escapados con `escHtml` en `report-liberator`, `wo-deadline-changer` (+`safeColor`), `inventory-reset`, `spec-migrator`, `bulk-upload`, `extension/popup.js`. `po-comparator`/`portal-importer` ya estaban limpios. Suite 62/0.
3. **Plan de rollback (MEDIO) — ✅ HECHO EN WORKBENCH.** `deploy.sh` crea tag `vX.Y.Z` por bump (aditivo/tolerante); `tools/rollback.sh <tag>` revierte gh-pages self-healing; doc [`docs/architecture/rollback.md`](rollback.md). Los tags son el CHANGELOG.
4. **CSP explícito (BAJO) — ✅ HECHO EN WORKBENCH (requiere republicar .zip para efecto).** `manifest.json` declara `content_security_policy.extension_pages: "script-src 'self'; object-src 'self'"`. Seguro: el `new Function` del remote-loader corre en `world:'MAIN'`, fuera del alcance de esa CSP.
5. **`console.log` en producción (BAJO) — ✅ HECHO (gate central; resto derivado).** Flag `sa_debug` (localStorage) / `config.debug`: `steelhead-api.js log()` gateado (lo usan 21+ applets; `warn` + persistencia intactos), `content.js` (por-carga) e `invoice-auto-regen` (poller, `console` sombreado local). Derivado: `console.log` directos de `weight-quick-entry`/`warehouse`/`paros-linea` (mismo patrón de shim).
6. **Anclajes bilingües ES+EN (audit) — ✅ MAPA COMPLETO; hardening bloqueado por evidencia del usuario.**
   **Actualización 2026-08-03 (`sensor-graph-hide-all`):** sus dos anclajes primarios son
   `aria-label` **exactos en inglés** (`'Hide this sensor in the graph.'` /
   `'Show this sensor in the graph.'`) y su único fallback era el `data-testid` — que SH
   eliminó. **Verificado en el DOM real del dashboard 117: los dos aria SIGUEN EN INGLÉS**, así
   que la primaria funciona hoy. Y el fallback dejó de ser un `data-testid` muerto: ahora es la
   **FORMA del icono** (`mui-icon-anchor-core`), así que aunque SH traduzca esos aria el applet
   no se queda sin red. La deuda baja de «sin red» a «acotada y con respaldo». 25 anclajes mono-idioma en 12 applets en [`docs/architecture/bilingual-anchoring-debt.md`](bilingual-anchoring-debt.md). **Alta 2026-07-29:** el candado de `warehouse-location-prefill` 0.6.x agregó 3 anclajes por texto con el ES verificado en vivo y el **EN como hipótesis** (`ROW_LOCATION_LABEL_RE`, `SAVE_BUTTON_RE`, `CANCEL_BUTTON_RE`). Se registran como deuda **aceptable y acotada** porque cada uno tiene fallback (estructural o por subcadena) y detrás está el candado del **payload**, idioma-independiente: la degradación es observable, no un apagado en silencio como el de `price-confirm-guard`. Una sola observación del modal en inglés cierra las tres. **Corrección 2026-07-16:** `surtido-guard` NO es riesgo de seguridad (se marcó P1 por error) — su candado es **API-driven** (`evaluateMove` vs `scheduledAccountIds` de `GetRelatedScheduleData`), bloquea en cualquier idioma; las cadenas de idioma solo alimentan el marcado cosmético (v0.2.0 naranja para no-movibles, ya **bilingüe ES+EN** + salvaguarda con el set de la API). La deuda restante son autofills/labels que dejan de dispararse (no seguridad). No se hardeniza sin el string real del otro locale (regla dura: no adivinar).
7. **Memory-hardening audit por applet (#113) — ✅ HECHO.** Estado de adopción de `host-cleanup-shared` en [`docs/applets/memory-hardening-audit.md`](../applets/memory-hardening-audit.md): 9 ADOPTADO, 5 PARCIAL, 2 NO-ADOPTADO (portal-importer, po-comparator), 0 INLINE-DUP. Adopción de los faltantes = follow-up no urgente.

**Nota (2026-07-15):** items 2-7 **DEPLOYADOS a producción, config 1.7.120, tag `v1.7.120`** (scripts remotos verificados en vivo). También se corrigió un test rojo pre-existente del `auto-router` (`destinationLines` seguía excluyendo la línea origen — bug Image #6 vivo). **Otros canales:** (a) los cambios de `extension/` (CSP en manifest, XSS en `popup.js`, gate en `content.js`) **NO se despliegan a la Chrome Web Store por diseño** — la extensión inyecta código remoto (remote script loader con `new Function`), incompatible con las políticas de MV3/CWS, así que **quedan en el repo sin desplegar** hasta que cambie esa arquitectura; no es un pendiente accionable hoy. (b) **bundle Safari/iPad v0.6.0 (2026-07-27, 32 applets, 1.41 MB, build-safari verde; sincronizado a Resources de Xcode)** — suma **`wo-schedule-button`** (nació POR el iPad: ahí la tarjeta Cliente con el 📅 nativo se colapsa, y aun así estaba FUERA del bundle), **`wo-listing-columns`** (columnas PN/Programación/Lote en el listado de OTs) y **`po-listing-filters`** (buscador global de OC). Lanzadores nuevos en popup+dispatcher: `open-wo-schedule`, `toggle-wo-pn-column`, `toggle-wo-sched-column`. Además toma los fixes del día (report-regen 0.3.4 con `MANAGE_REPORTING_SETTINGS`, pn-specs-column). **CRITERIO CORREGIDO:** el scanner marcaba los dos primeros como NO-APLICA por detectar `a.download`, pero esa descarga es el **PDF — función lateral y opt-in**, no su flujo core. *NO-APLICA solo aplica cuando el FLUJO CORE del applet es la descarga* (auditor, carga-masiva, file-uploader…); si la descarga es una función más, el applet sí va al bundle. **v0.6.3 (2026-07-28, 32 applets, sin altas)** — rebuild para tomar `price-confirm-guard`/`price-confirm-core` con el **gate estructural** y, sobre todo, el reconocimiento del `alert` nativo en español «Error al guardar el precio»: ese alert es **bloqueante**, y en iPad sacudir un diálogo modal encima de la SPA cuesta más que en escritorio. **Salió a 0.6.3 y no a 0.6.2 porque las DOS sesiones en paralelo bumpearon a 0.6.2 por separado** (la otra con el FAB 📦 del auto-router 0.3.2): al mergear, `bundle.json` chocó y `main-bundle.js`/`manifest.json` se resolvieron **REGENERANDO** con `build-safari.sh` sobre las fuentes ya mezcladas —son artefactos, no se editan a mano— verificando después que el artefacto trajera los dos cambios (`isPriceModal` ×6 + lanes ×11). **Lección de trabajo paralelo: el bundle es un hot file más, como `config.json` y `CLAUDE.md`.** **v0.6.29 → 0.6.30 (2026-08-04, 32 applets, sin altas) — la tanda de «TX00 es un ÁREA».** `0.6.29` lleva `surtido-guard` **0.4.1** (el filtro por línea destino distingue `T300-CE03` de `T300-CE05`) y `0.6.30` lleva `auto-router` **0.4.1** (la misma regla en el **bypass** del motor de ruteo). Los dos applets ya estaban en la lista blanca y no hubo core nuevo ⇒ bump + rebuild, sin lanzadores. **El iPad es el punto de uso de ambos**: el almacén surte desde ahí y la planificación —incluido el ✂️ de partir piezas desde el tablero— también se hace ahí. Verificado **en el artefacto**: `AREA_CODE_RE` **0 → 2** (filtro) **→ 4** (filtro + motor); 1 780 870 → **1 783 642 bytes** (bytes reales de `wc -c`), `node --check` OK y build-safari 10/10 en ambos. `Resources/` de Xcode sincronizado (md5 idénticos). **La tercera pata de esa tanda NO requirió rebundle** y se verificó, no se supuso: `process-shared`/`process-canon`/`process-deep-audit` **no están en el bundle** (`bundle.json` no lista ninguna app de procesos). **Falta recompilar en Xcode** — un solo build toma las dos. **v0.6.20 → 0.6.27 (2026-08-03, 32 applets, sin altas) — la tanda del día del «SH cambió sus anclas»; el último, `0.6.27`, deja el bundle con el CATÁLOGO DE ICONOS COMPLETO (14 formas).** `0.6.20` lleva el reanclaje del **modal de recepción** (`receive-modal-anchor-core` + RDO/WLP): el iPad es el dispositivo con el que el almacén RECIBE, así que ahí el ancla muerta dolía el doble — sin el combo del encabezado la ubicación se ponía renglón por renglón, con teclado en pantalla. `0.6.21` lleva **`mui-icon-anchor-core` + `report-regen` 0.3.5**. `0.6.22` lleva el **barrido de los 8 applets restantes** por ese mismo núcleo. Verificados **en el artefacto** con baseline 0 en cada símbolo (`ReceiveModalAnchorCore`, `mountHeaderField`, `findReportHeaderAnchor`, `ICON_ARIA`, `findIcons`, `hasAnyIcon`, `saHasIcon`, `hasSendOrEmailIcon`); 1 748 059 → 1 756 340 → **1 770 068 bytes** (bytes reales de `wc -c`, no los caracteres que imprime el script), `node --check` OK y build-safari 17/17 en los tres. `Resources/` de Xcode sincronizado (md5 idénticos). Los cinco siguientes siguen el mismo patrón —baseline 0 → presencia verificada **en el artefacto**, `node --check` OK, `build-safari --check` verde—: `0.6.23` re-espeja los cores, `0.6.24` la verificación por applet, `0.6.25` Close + Edit-outlined + Print, `0.6.26` Send + Visibility + VisibilityOff, y `0.6.27` RestorePage (1 776 464 bytes). **Único pendiente de TODA la tanda: recompilar en Xcode.** **Aquí el iPad no es un extra:** los applets del modal de recepción y el listado de OTs son de PISO, y estaban rotos en silencio. **v0.6.19 (2026-08-03, 32 applets, sin altas)** — rebuild para `schedule-batch-highlighter` **0.2.0**: entran sus DOS scripts nuevos (`schedule-batch-group-core.js` + `schedule-batch-group.js`), el **📦 que agrupa las órdenes de un lote en UNA tarea del programa**. **Es el único rebundle del día que aporta algo**: el fix del popup de 800px es de `extension/` de Chrome (Safari tiene su propio popup) y `wo-spec-params` 0.6.0 no está en el bundle (su app `spec-migrator` es NO-APLICA por descarga) — sin el agrupador, el artefacto no habría cambiado un byte. **DECISIÓN EXPLÍCITA DEL USUARIO contra lo que decía la bitácora**: se había dejado fuera a propósito hasta la primera corrida real porque **escribe el programa** y el iPad es el dispositivo de piso; se integró a petición suya ⇒ **la corrida real sigue pendiente y ahora es lo primero a vigilar**. Verificado **en el artefacto** (baseline **0** en las tres): `ScheduleBatchGroupCore` ×2, `buildBatchGroups` ×3, `buildGroupedScheduleTaskInput` ×3, 4 bloques `BEGIN/END`; **1 736 732 bytes** (antes 1 688 217 — bytes reales, no los caracteres que imprime el script). `node --check` OK, build-safari 10/10. `Resources/` de Xcode sincronizado (md5 idénticos); **falta recompilar en Xcode**. **v0.6.18 (2026-07-30, 32 applets, sin altas)** — rebuild para llevar al iPad `surtido-guard` **0.4.0**, el fix del FALSO POSITIVO del candado. **El iPad es el punto de uso**: el almacén surte desde ahí, así que el bug —bloquear material que SÍ estaba programado— pegaba justo donde duele. El applet ya estaba en la lista blanca y no hubo core nuevo (la lógica entró en `surtido-guard-core.js`), así que bastó bump + rebuild (sin lanzador nuevo). Verificado **en el artefacto**, no en el log: `buildAccountScheduleMap` ×3, `isPartsInProcessOp` ×3, `resolveAccountSchedule` ×4, `accountScheduled` ×10, `scheduledKnownCount` ×4 y la nota ámbar «no pudo verificar la programación» ×1 — los cinco primeros en **0** antes del rebuild; 1 654 137 bytes, `node --check` OK. `Resources/` de Xcode sincronizado (md5 idéntico al de `safari/extension/`); **falta recompilar en Xcode**. **v0.6.17 (2026-07-30, 32 applets, sin altas)** — rebuild para llevar al iPad los dos fixes del modal de recepción: el candado de `warehouse-location-prefill` **0.6.2** (Enter/Tab/blur resuelven la ubicación tecleada) y `weight-quick-entry` **0.5.82** (el peso rápido llega a TODAS las filas). **Aquí el iPad importa más que el escritorio**: es el dispositivo con el que el almacén recibe material, y con teclado en pantalla teclear la ubicación y dar Tab es más común que atinarle a la opción del dropdown. Verificado **en el artefacto**: `resolveTypedLocation` ×4, `commitTypedLocation` ×6, `sa-wlp-typed-warn` ×2, `startLinePoll` ×2, `detectTick` ×2, `_saWqeLinePoll` ×5 — los seis en **0** antes del rebuild; 1 681 092 bytes, `node --check` OK, build-safari 10/10. Sincronizado a `Resources/`; **falta recompilar en Xcode**. **v0.6.16 (2026-07-30, 32 applets, sin altas)** — rebuild con el indicador animado de carga del catálogo (`sa-sg-ring` ×4, `boardCatalogState` ×8 en el artefacto). **v0.6.15 (2026-07-30, 32 applets, sin altas)** — rebuild con el catálogo de líneas desde la API (`linesFromBoardSchedule` ×3, `ensureBoardLineCatalog` ×2, `boardLines` ×7 en el artefacto). **v0.6.14 (2026-07-30, 32 applets, sin altas)** — rebuild inmediato tras el fix del dropdown acumulativo (`accumulateSeenLines`); verificado en el artefacto: `accumulateSeenLines` ×4, `seenLines` ×6, `node --check` OK, sincronizado a `Resources/`. **v0.6.13 (2026-07-30, 32 applets, sin altas)** — rebuild para llevar al iPad el **filtro por LÍNEA DESTINO** de `surtido-guard` **0.3.0** con su núcleo nuevo `surtido-guard-filter-core.js`. El applet ya estaba en la lista blanca y `build-safari.sh` expande `config.apps[].scripts`, así que bastó bump + rebuild (sin lanzador nuevo). **El iPad es donde esto más pesa**: es el dispositivo con el que el almacén surte, y filtrar por la línea a la que va el material es justo la tarea de piso. Verificado **en el artefacto**: `linesFromScheduledRows` ×3, `mergeLineCatalog` ×3, `lineCodeFromStationText` ×4, `sa-sg-filter` ×18 y `Línea destino` ×2 — los cinco en **0** antes del rebuild; 1 658 675 bytes, `node --check` OK. Sincronizado a `Resources/`; **falta recompilar en Xcode**. **v0.6.12 (2026-07-29, 32 applets, sin altas)** — rebuild para llevar al iPad el **candado de ubicación** de `warehouse-location-prefill` **0.6.1** con su núcleo nuevo `warehouse-location-guard-core.js`. El applet ya estaba en la lista blanca y `build-safari.sh` expande `config.apps[].scripts`, así que bastó bump + rebuild (sin lanzador nuevo). **Aquí el iPad importa MÁS que el escritorio**: es el dispositivo con el que el almacén recibe material, y sin el core el candado quedaba apagado por fail-open justo en el punto de uso. Verificado **en el artefacto**: `rowHasLocation` ×5, `ROW_LOCATION_LABEL_RE` ×4, `payloadMissingLocations` ×4, `sa-wlp-gate-note` ×4, `data-sa-wlp-blocked` ×1 — los cinco en **0** antes del rebuild; 1 633 559 bytes, `node --check` OK, build-safari 10/10. Sincronizado a `Resources/`; **falta recompilar en Xcode**. **v0.6.11 (2026-07-29, 32 applets, sin altas)** — rebuild para `batch-name-filter` **0.3.1**: fuera el aviso de «vacío» (un lote es un CONTENEDOR que SH VACÍA al pasarlo a OT ⇒ vacío es el curso NORMAL y el lote sirve como REFERENCIA; el aviso salía casi siempre y leía como problema lo esperado), **marcado INVERTIDO** (se resalta el que AÚN tiene material — la excepción y lo único enviable) y tope de paginación 600→**1600** porque los lotes vacíos se ACUMULAN históricamente. Verificado en el artefacto: `aún con material` ×2 y `MAX_TOTAL_TO_PAGE = 1600` ×1, ambos en **0** antes; las 2 ocurrencias restantes del patrón viejo son el COMENTARIO que documenta la retirada, no código (`function shouldWarnAllDepleted` = 0 — **buscar la función, no la frase**). build-safari 10/10. **v0.6.10 (2026-07-29, 32 applets, sin altas)** — rebuild para `batch-name-filter` **0.3.0** (los dos universos de `hideCompleted`); en el iPad pesa doble porque el operador de piso busca lotes VIEJOS, que son justo los que caían en el universo escondido. Ambos sincronizados a `Resources/`; **falta recompilar en Xcode**. **v0.6.9 (2026-07-29, 32 applets, sin altas)** — rebuild para `pn-specs-column` **0.3.2** (Specs al final de la barra, racks en 2 renglones, unidades en el orden del ERP, nombre del NP a 14px/700) + el fix del `<style>` que se recreaba en cada sync. Verificado en el artefacto: `UNIT_ORDER` ×5, `sa-pncol-rack-qty` ×3, `sa-pn-active` ×3 y **cero** ocurrencias del `'2'` viejo; 1 591 303 bytes, build-safari 10/10. **v0.6.8 (2026-07-29, 32 applets, sin altas)** — rebuild para `pn-specs-column` **0.3.1** (angostar: sin columna de Línea, desc+metal dentro de la celda del nombre, racks `T102-RA02 (18 pz)`, unidades a 3 decimales con el `/pz` en el encabezado). En el iPad el ancho pesa más que en escritorio. Verificado en el artefacto: `fmtQty3` ×5, `sa-pncol-nameinfo` ×3, `formatRackChip` ×3, `sa-pncol-linea` **×0** (lo retirado no viaja); 1 587 695 bytes, build-safari 10/10. **v0.6.7 (2026-07-29, 32 applets, sin altas — el scanner dio 0 integrables)** — rebuild que salda DOS pendientes de golpe: `pn-specs-column` **0.3.0** (de 1 a 5 columnas al inicio de `/PartNumbers`) y `wo-schedule-button` **0.9.0** (programar por tratamiento ancla), este último arrastrado desde el rebundle anterior. Ambos ya estaban en la lista blanca, así que bastó reconstruir. Verificado **en el artefacto**, no en el log: `extractPnRow` ×3, `extractUnitFactors` ×3, `fmtFactor` ×4, `sa-pncol-metal` ×2 y `pickAnchorSteps` ×3 — los cinco en **0** antes del rebuild. 1 579 883 bytes (antes 1 548 318); `node --check` OK, build-safari 10/10, suite 83/83. `Resources/` de Xcode sincronizado; **falta recompilar en Xcode**. **v0.6.6 (2026-07-28, 32 applets, sin altas)** — rebuild para llevar al iPad el ✂️ del auto-router en el tablero (0.4.0) y `wo-schedule-button` hasta **0.8.0** (programar desde la ficha); dejó fuera el 0.9.0 a propósito porque otra sesión estaba activa sobre este worktree y `bundle.json` es hot file. **v0.6.4 (2026-07-28, 32 applets, sin altas)** — rebuild para llevar `auto-router` 0.4.0 al iPad: el ✂️ de partir piezas ahora también vive en el tablero de planificación, que es justo donde se planifica desde el iPad. (Al verificar el artefacto: `build-safari.sh` imprime **caracteres**, no bytes — el bundle "encogió" de 1483650 a 1462283 sin haber quitado nada; en bytes reales creció a 1492079. Compara bloques `BEGIN/END`, no el número que imprime el script.) v0.5.9 (2026-07-23, 29 applets) había integrado `batch-name-filter` y `schedule-batch-highlighter`. **LECCIÓN operativa nueva:** el **Modo de Aislamiento** de iPadOS («modo hermético» / Lockdown Mode) debe quedar **APAGADO** en el iPad — verificado en piso: con él activo `app.gosteelhead.com` **ni carga**; no confundir con el **Modo Desarrollador**, que va ON (ver `safari/README.md` §Estado y `docs/deploy-safari.*` Paso 3). Solo falta **recompilar en Xcode**.

### No aplica (por arquitectura)
- Auth / sessions / HTTPS / DB / CORS / rate limiting / bcrypt / backups / migrations: no hay backend propio; consumimos Steelhead GraphQL vía cookies de sesión del navegador.

Checklist completa pre-producción vive en `~/.claude/CLAUDE.md` (global del usuario).
